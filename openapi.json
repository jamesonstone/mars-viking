{
  "openapi": "3.0.0",
  "info": {
    "title": "Mars Viking",
    "description": "A Test Application for Mars Command",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "http://api.example.com/v1",
      "description": "Optional server description, e.g. Main (production) server"
    }
  ],
  "paths": {
    "/testjson": {
      "get": {
        "summary": "Returns a random set of json",
        "description": "Optional extended description in CommonMark or HTML.",
        "responses": {
          "200": {
            "description": "A JSON object of random things",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
